# Bridge runtime and headroom pass — 2026-09-13

**Status: substantial verified improvement; steady 60 FPS with expansion headroom is not yet achieved.**
The owner asked for 60 FPS at the present scale and room for larger battles.
This checkpoint follows 613bbabb4d7d642bdaeb008e4ad272530ff53fd5 on build/arsenal-checkpoint-20260911.

## Approved release-runtime comparison

Brandon approved downloading and executing the official matching runtime on
2026-09-13. The runtime reports Godot 4.7.2 official hash
ed1daf0bf001b61586d9930840f2f1394092c079, debug=false and editor=false.
Archive CRC was checked; the extracted binary SHA256 and public source are in
[release_provenance.json](headroom/release_provenance.json).
No production gameplay, artwork, cadence, limits or project settings changed in
this comparison.

| Sample | FPS | P95 ms | Worst ms |
| --- | ---: | ---: | ---: |
| Release 24, normal | 59.87 | 16.910 | 40.125 |
| Release 32, normal | 58.66 | 19.803 | 44.322 |
| Release 24, uncapped | 85.59 | 16.978 | 35.539 |
| Development 24, same pack uncapped | 73.33 | 15.905 | 64.674 |
| Release 32, uncapped, degraded batch | 34.36 | 36.560 | 83.292 |
| Development 32, same degraded batch | 26.95 | 46.349 | 89.561 |
| Release 32, uncapped repeat | 63.64 | 19.185 | 47.342 |
| Release 32, normal repeat | 59.78 | 17.031 | 40.182 |

These are 30-second samples at 1440 x 1000. Normal samples used the measured
60 Hz display with VSync; uncapped samples removed that limit only for the test.
All selected actors rendered, combat inflicted damage, and all accepted native
samples had zero frames over 100 ms.

The same-package 24-unit uncapped pair averaged 73.33 FPS in the
development executable and 85.59 FPS in release. Release P95 was
16.978 ms versus 15.905 ms in development:
the average improvement did not improve the tail in this pair. Variable frame
steps and casualties change battle evolution; this is not an equal-work causal
speedup measurement. The release 24-unit opening five-second window was only
57.35 FPS. Do not equate the overall average with full-roster capacity.

The later 32-unit uncapped batch deteriorated sharply, including setup and
paused rendering. It remains recorded, but cannot support a dependable capacity
claim. A bounded repeat recovered to 63.64 FPS, with P95
19.185 ms and five-second windows below 60. The normal
repeat measured 59.78 FPS, P95 17.031 ms.
AC power and healthy GPU clocks were observed; process samples also showed
background OneDrive sync activity. Those snapshots do not prove the cause of
the earlier degraded batch. See [repeat telemetry](headroom/release_repeat_telemetry.json).

**Conclusion:** release throughput is useful, but steady 60 FPS with expansion
headroom is still unproven. The 32-unit frame-time tail still exceeds 16.67 ms.
No maximum legal personnel/convoy cap is changed or approved by these fixtures.
The current 24-unit and 32-unit fixtures also differ in convoy composition.

### Release validation and reproduction

All 11,022 checks passed again in release: 8,605 focused checks, 1,215 replay
comparisons and 1,202 actor checks across 24 actors and 11 observed clips.
Their explicit check functions remain active in release; they do not rely on
GDScript assert statements. Every final selected run passed the engine/script
error gate. This does not re-establish the legacy whole-project core suite or
owner acceptance.

The official template rejects path/script command-line overrides, as described
in the [Godot command-line documentation](https://docs.godotengine.org/en/latest/tutorials/editor/command_line_tutorial.html).
The benchmark uses a small launcher PCK and an adjacent data PCK containing the
existing source/imported assets. Report destinations and test bootstrap differ;
the production project settings file stays untouched. Both executables use the
same package for the paired samples. This is a benchmark package, not a finished
shipping export.

The base package contains 20,127 resource files (about 4.29 GB before packing)
and took 125.25 seconds to prepare on this PC. Byte-range download failed with
HTTP 501; ordinary archive download succeeded in 14.14 seconds. The first
packaged native trial failed on a missing root icon.svg and was excluded;
the corrected launcher includes it. These preparation/failed-attempt costs
are separate from combat frame times.

[release_results.json](headroom/release_results.json) retains all accepted
samples and caveats. The [README](headroom/README.md) explains the two build/run
scripts and the bounded repeat. [Source metadata](headroom/release_source_snapshot.json)
records HEAD plus the inherited runtime working-tree diff hash. The procedure
uses the live working tree and imported assets; it is not a claim that a clean
checkout alone reproduces this exact mixed-working-tree snapshot.

## Earlier development-runtime results

| Fixture | Average FPS | P95 frame ms | Worst frame ms | Frames over 100 ms |
| --- | ---: | ---: | ---: | ---: |
| 24, normal | 59.05 | 19.143 | 46.517 | 0 |
| 32, normal | 48.55 | 24.651 | 58.916 | 0 |
| 24, same Bulwark convoys | 59.03 | 19.249 | 48.512 | 0 |
| 24, uncapped | 61.31 | 19.419 | 41.732 | 0 |
| 32, uncapped | 55.03 | 21.397 | 56.259 | 0 |

The previously verified 24-unit checkpoint measured 42.34 FPS, P95 31.842 ms
and max 292.985 ms; the older pre-cleanup baseline measured 30.15 FPS. These are
successive development-PC samples, not a claim of a perfectly controlled GPU
benchmark. Current normal runs rendered every selected actor and inflicted damage.
Frame times still exceed the 16.67 ms budget. Do not describe 59 FPS or the
uncapped average as proof of steady 60 FPS or adequate expansion headroom.

The 32-unit fixture uses three Bulwarks on each side. The 24-unit Bulwark run
separates that convoy/layout difference from unit count. Test-only force limits
do not change the production sandbox's 12-per-side limit or approve a new
convoy/personnel cap. Results depend on machine load, renderer and variable-frame
battle evolution. The renderer remains D3D12 Forward+; a Compatibility experiment
was slower (44.75 FPS at that stage) and was rejected.

The final matched 600-update replay took
8370.499 ms in the original runtime versus
5409.228 ms in the current runtime.
It compares equal work and recorded outcomes, not native rendering FPS.

## Causes and changes

- Target selection repeatedly validated every source/candidate pair and checked
  LOS before cheaper ordering rules. Eligible rows are prepared once; assault
  and sniper checks follow the existing priority/distance rules with identical
  tie behavior and live eligibility.
- Weapon/tier/role profiles, vehicle transforms, sorted vehicle IDs, readiness
  and vehicle topology strings were repeatedly reconstructed. Synchronous scopes
  reuse stable data and restore the preceding scope before the next input/update.
  Death, damage, orders and other live combat state remain live.
- Navigation connected each new endpoint to a large visibility graph. Known cover
  endpoints are prepared before combat. Conservative rectangle hierarchies prune
  obstacle checks; a legal seed route bounds endpoint candidates, with a full
  fallback. Exact slab predicates and route behavior are retained. Reachability
  reuses existing connectivity and prepared endpoints.
- Cover ranking duplicated protection work and checked LOS before rejecting
  slots for unchanged range/progress rules. Those cheap rejections now happen
  first; standalone evaluation remains complete.
- LOS considered many obstacles in a segment's broad bounding rectangle.
  A conservative ray hierarchy narrows candidates, retaining exact nearest-hit,
  interior-edge and obstacle-ID tie rules. Its live shape snapshot is checked
  once per synchronous scope, including direct edits between scopes.
- LOS and reachability memoization grew without bound as moving endpoints changed.
  Two generations now cap retained entries at 16,384 LOS and 8,192 reachability
  rows, retain useful recent queries and clear both generations on topology edits.
- First-wound anchor JSON and clothing-mask loading caused presentation stalls.
  Selected actors' assets are prepared during setup. Shot history is indexed once
  per actor refresh instead of rescanned per actor. Art and animation assets are
  unchanged.

A native 32-unit presentation profile at an intermediate stage attributed about
14.2 ms/update to simulation and about 2.5 ms/frame to actor presentation plus
dynamic drawing. Profiling numbers include measurement overhead. The remaining
limit is substantially simulation work, not sprite count alone.

Preparation moves work into setup; it is not free. The later runs record
setup times of 20.90 s (24 mixed convoy),
22.05 s (24 Bulwarks), and
28.57 s (32 Bulwarks). These include scene/asset loading
and the one-second fixture wait, not solely navigation preparation.

## Validation

11,022 checks passed with zero final script/engine errors:
- 8,605 focused checks: randomized/tangent movement and LOS,
  exact closest blocker, uncached routes, target/priority edge cases, scoped
  collision/profile ownership, live invalidation and bounded cache rollover.
- 1,215 full-runtime comparisons: positions, health,
  cover/movement modes, targets, ammo/timers, RNG and the deciding kill.
- 1,202 actor comparisons across 24 actors and 11 observed
  animation clips, including cover, wounds and death; 24 masks prepared.

The reference-preparation utility reproduced all 39 tested reference files.
The historic whole-project core regression suite was not re-established here;
do not imply it is green. Raw diagnostic experiments/backups remain local.
Reproducible harnesses, compact evidence and final logs accompany this checkpoint.

## Immediate continuation

Profile the 32-unit release battle during its slow windows, isolate the remaining simulation cost from system-load variation, and remove that cost while preserving combat behavior. Judge the next change by frame-time tails and full-roster windows, not the overall average alone.
